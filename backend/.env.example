# ── Miljö ────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000

# ── URLer ────────────────────────────────────────────────────
# Utan trailing slash
FRONTEND_URL=https://dykgaraget.se
API_URL=https://dykgaraget.se/api

# ── Databas ──────────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dykgaraget
DB_USER=dykgaraget_user
DB_PASSWORD=BYTA_UT_DETTA_LÖSENORD

# ── JWT ───────────────────────────────────────────────────────
# Minst 32 tecken, slumpmässig sträng
# Generera: openssl rand -base64 48
JWT_SECRET=BYTA_UT_DETTA_TILL_MINST_32_SLUMPMÄSSIGA_TECKEN
JWT_EXPIRES_IN=7d

# ── Admin-lösenord (sätts vid npm run migrate) ───────────────
# Ändra detta INNAN du kör migrate första gången
ADMIN_PASSWORD=Admin123!

# ── SendGrid (valfritt - krävs om feature_email = true) ──────
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=noreply@dykgaraget.se
EMAIL_FROM_NAME=Dykgaraget

# ── Stripe (valfritt - krävs om feature_payment = true) ──────
STRIPE_SECRET_KEY=sk_test_eller_sk_live_xxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxx

# ── Fillagring ───────────────────────────────────────────────
UPLOAD_DIR=./uploads
